VITE_API_BASE_URL=http://localhost:8000
# Set to true only when intentionally reviewing the built-in sample analysis.
VITE_DEMO_MODE=false
# Optional: port `npm run dev` starts the FastAPI backend on (see scripts/dev.mjs).
BACKEND_PORT=8000
