# Optional AI provider dependency (Gemini).
# Install with:  pip install -r requirements-ai.txt
# The backend runs without this; the AI layer degrades gracefully when the
# package or GEMINI_API_KEY is absent.
google-genai>=0.3

# Used for RETRIEVAL_MODE=pgvector (Postgres + pgvector). Harmless when unused.
psycopg[binary]>=3.1
pgvector>=0.2
